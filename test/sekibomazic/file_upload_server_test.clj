(ns sekibomazic.file-upload-server-test
  (:require [clojure.test :refer :all]
            [sekibomazic.file-upload-server :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
